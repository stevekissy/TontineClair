// Stub — intégration CoinPayments supprimée.
// Le paiement automatique Crypto n'est plus disponible.
// Toutes les opérations passent par le paiement manuel (PaiementChoixScreen).

class CoinPaymentsService {
  // Service désactivé — aucune méthode API disponible.
  const CoinPaymentsService._();
}

class CoinPaymentsResultat {
  const CoinPaymentsResultat._();
}
