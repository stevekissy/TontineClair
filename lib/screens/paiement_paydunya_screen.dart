// Stub — intégration PayDunya supprimée.
// Le paiement automatique Mobile Money n'est plus disponible.
// Utiliser PaiementChoixScreen pour le paiement manuel.
import 'package:flutter/material.dart';

class PaiementPayDunyaScreen extends StatelessWidget {
  const PaiementPayDunyaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
