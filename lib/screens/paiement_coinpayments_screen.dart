// Stub — intégration CoinPayments supprimée.
// Le paiement automatique Crypto n'est plus disponible.
// Utiliser PaiementChoixScreen pour le paiement manuel.
import 'package:flutter/material.dart';

class PaiementCoinPaymentsScreen extends StatelessWidget {
  const PaiementCoinPaymentsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
