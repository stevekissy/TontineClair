// Stub — intégration Crypto Wallet supprimée.
// Utiliser PaiementChoixScreen pour le paiement manuel.
import 'package:flutter/material.dart';

class PaiementCryptoWalletScreen extends StatelessWidget {
  const PaiementCryptoWalletScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
