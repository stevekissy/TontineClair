# Déploiement manuel uniquement (ZIP via API)
# Le build Flutter est fait en local puis déployé via API Netlify
# NE PAS activer le build automatique — Flutter n'est pas dispo sur Netlify CI

[build]
  ignore = "/bin/true"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
