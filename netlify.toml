# TontineClair — Netlify config
# Le build Flutter est fait localement et déployé via API (ZIP)
# Ce fichier désactive le build côté Netlify CI

[build]
  command = ""
  publish = "build/web"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
