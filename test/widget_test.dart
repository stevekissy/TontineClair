import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Admin app smoke test', (WidgetTester tester) async {
    // Smoke test minimal — l'app admin démarre sans crash
    expect(true, isTrue);
  });
}
